# M1-P7 前端最小可玩(Vanilla JS + PixiJS + PWA 壳)实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development。步骤用 `- [ ]`。

**Goal:** 用 Vanilla HTML/CSS/JS ES Modules + PixiJS 做一个浏览器可玩的最小前端:登录/注册 → 等距地图看城市 → 从建筑面板选建筑点地建造 → HUD 显示资源并每 ~10 秒轮询快照看生产。前端只提交意图,服务器权威。

**Architecture:** 静态前端放 `public/game/`,由 Laravel 同源提供;认证走 Session Cookie(先 GET /api/csrf-cookie 取 XSRF-TOKEN,之后所有写请求带 `X-XSRF-TOKEN` 头 + `credentials:'include'`)。渲染层 PixiJS 只画地图/建筑,DOM 负责 HUD/面板;游戏规则在服务器。PixiJS 以本地静态文件单文件引入(不走 CDN)。

**Tech Stack:** Vanilla JS ES Modules,PixiJS(本地 UMD 全局 `PIXI`),无构建工具。后端承接 P2 认证 / P4 快照 / P5 建造。

## Global Constraints
- 无 React/Vue/TS/Vite;`<script type="module">` 直接加载;文件 UTF-8 无 BOM,LF。
- 客户端不可信:不本地计算资源结果,只提交 `{buildingId,x,y}`,用服务器返回的 diff 更新状态。
- CSRF:写请求(POST)必须带 `X-XSRF-TOKEN`(从 `XSRF-TOKEN` cookie 读并 `decodeURIComponent`)。
- 中文注释;模块单一职责(CLAUDE §6 目录);不把每个地图建筑做成 DOM 元素(建筑由 PixiJS 画)。
- 本地起服:`"C:/xampp/php/php.exe" artisan serve --port=8127`;浏览器验证用 Chrome。

---

### Task 1: 后端支持——可建建筑列表端点

**Files:**
- Create: `app/Http/Controllers/City/DefinitionController.php`
- Modify: `routes/web.php`(auth:web 组内 `GET /api/definitions/buildings`)
- Test: `tests/Feature/City/DefinitionApiTest.php`

**Interfaces:**
- `GET /api/definitions/buildings`(auth:web)→ `{success:true,data:{buildings:[{buildingId,name,category,era,footprint:{w,h},maxCount,level1:{cost:{},output:[]}}]}}`
  - 联 `building_definition` + `building_level_definition`(level=1)取 L1 成本与产出,供前端建筑面板显示。

- [ ] **Step 1: 写失败测试** `tests/Feature/City/DefinitionApiTest.php`

```php
<?php

namespace Tests\Feature\City;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class DefinitionApiTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void { parent::setUp(); $this->seed(); }

    public function test_lists_buildable_buildings(): void
    {
        $u = User::create(['username' => 'defviewer', 'name' => 'defviewer', 'email' => 'd@v.com', 'password' => 'password123']);
        $res = $this->actingAs($u)->getJson('/api/definitions/buildings');
        $res->assertOk();
        $res->assertJsonStructure(['data' => ['buildings' => [['buildingId', 'name', 'footprint' => ['w', 'h'], 'level1' => ['cost']]]]]);
        // 94 座
        $this->assertCount(94, $res->json('data.buildings'));
    }

    public function test_requires_auth(): void
    {
        $this->getJson('/api/definitions/buildings')->assertStatus(401);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 写 `DefinitionController.php`**

```php
<?php

namespace App\Http\Controllers\City;

use App\Http\Controllers\Controller;
use App\Support\ApiResponse;
use Illuminate\Http\JsonResponse;
use Illuminate\Support\Facades\DB;

// 定义只读:可建建筑列表(带 L1 成本/产出)
class DefinitionController extends Controller
{
    public function buildings(): JsonResponse
    {
        $defs = DB::table('building_definition as bd')
            ->join('building_level_definition as bl', function ($j) {
                $j->on('bd.building_id', '=', 'bl.building_id')->where('bl.level', '=', 1);
            })
            ->select('bd.building_id', 'bd.name', 'bd.category', 'bd.era_key', 'bd.max_count',
                'bd.footprint_w', 'bd.footprint_h', 'bl.cost_json', 'bl.output_json')
            ->orderBy('bd.building_id')
            ->get()
            ->map(fn ($r) => [
                'buildingId' => $r->building_id,
                'name'       => $r->name,
                'category'   => $r->category,
                'era'        => $r->era_key,
                'maxCount'   => (int) $r->max_count,
                'footprint'  => ['w' => (int) $r->footprint_w, 'h' => (int) $r->footprint_h],
                'level1'     => [
                    'cost'   => json_decode($r->cost_json, true),
                    'output' => json_decode($r->output_json, true),
                ],
            ])->all();

        return ApiResponse::ok(['data' => ['buildings' => $defs]]);
    }
}
```

- [ ] **Step 4: 路由**(auth:web 组内):
```php
        Route::get('/definitions/buildings', [\App\Http\Controllers\City\DefinitionController::class, 'buildings']);
```

- [ ] **Step 5: 跑测试通过 + Commit** — `git add app/Http/Controllers/City/DefinitionController.php routes/web.php tests/Feature/City/DefinitionApiTest.php && git commit -m "M1P7 可建建筑列表端点"`

---

### Task 2: 前端骨架 + 认证 + HUD(DOM,先不接 PixiJS)

**Files:**
- Create: `public/game/index.html`
- Create: `public/game/css/base.css`、`css/hud.css`、`css/auth.css`、`css/panels.css`
- Create: `public/game/js/main.js`
- Create: `public/game/js/core/api.js`、`core/state.js`、`core/config.js`
- Create: `public/game/js/ui/auth.js`、`ui/hud.js`
- Create: `public/game/js/utils/format.js`

**Interfaces:**
- `api.js`:导出 `api.get(path)`、`api.post(path, body)`;自动处理 CSRF(首次 `ensureCsrf()` 调 `/api/csrf-cookie`,读 `XSRF-TOKEN` cookie),`credentials:'include'`;失败抛 `{status, error}`。
- `state.js`:导出 `state`(`{user, city}`)与简单订阅 `onChange(cb)`。
- `auth.js`:渲染登录/注册表单,调 `/api/auth/register` `/api/auth/login`,成功后 `bootApp()`。
- `hud.js`:渲染资源/资金/人口/revision,`hud.update(city)`。
- `main.js`:启动 → `/api/me` 判断是否已登录 → 已登录 `bootApp()` 拉 `/api/city` 显示 HUD;否则显示 auth。

- [ ] **Step 1: 写 `index.html`**(挂载点 + 引 main.js `type=module`):
```html
<!doctype html>
<html lang="zh">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  <title>城市建设</title>
  <link rel="stylesheet" href="/game/css/base.css">
  <link rel="stylesheet" href="/game/css/auth.css">
  <link rel="stylesheet" href="/game/css/hud.css">
  <link rel="stylesheet" href="/game/css/panels.css">
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/game/js/main.js"></script>
</body>
</html>
```

- [ ] **Step 2: 写 `core/api.js`**(CSRF + fetch 封装)

```js
// 统一 API 封装:同源 Session + CSRF;客户端只提交意图
const BASE = '';

// 读 cookie
function cookie(name) {
    const m = document.cookie.match(new RegExp('(?:^|; )' + name + '=([^;]*)'));
    return m ? decodeURIComponent(m[1]) : null;
}

let csrfReady = false;
async function ensureCsrf() {
    if (csrfReady) return;
    await fetch(BASE + '/api/csrf-cookie', { credentials: 'include' });
    csrfReady = true;
}

async function request(method, path, body) {
    if (method !== 'GET') await ensureCsrf();
    const headers = { 'Accept': 'application/json' };
    if (body) headers['Content-Type'] = 'application/json';
    const token = cookie('XSRF-TOKEN');
    if (method !== 'GET' && token) headers['X-XSRF-TOKEN'] = token;

    const res = await fetch(BASE + path, {
        method, headers, credentials: 'include',
        body: body ? JSON.stringify(body) : undefined,
    });
    const json = await res.json().catch(() => ({}));
    if (!res.ok || json.success === false) {
        throw { status: res.status, error: json.error || 'REQUEST_FAILED', body: json };
    }
    return json.data;
}

export const api = {
    get: (p) => request('GET', p),
    post: (p, b) => request('POST', p, b),
};
```

- [ ] **Step 3: 写 `core/state.js`**、`core/config.js`、`utils/format.js`

`state.js`:
```js
// 简单客户端状态 + 订阅
export const state = { user: null, city: null, definitions: null };
const subs = [];
export function onChange(cb) { subs.push(cb); }
export function setState(patch) { Object.assign(state, patch); subs.forEach((cb) => cb(state)); }
```
`config.js`:
```js
export const CONFIG = { pollMs: 10000, tileW: 48, tileH: 24 }; // 等距瓦片尺寸
```
`utils/format.js`:
```js
export function fmt(n) { return Math.round(Number(n) || 0).toLocaleString('zh-CN'); }
```

- [ ] **Step 4: 写 `ui/auth.js`**(登录/注册表单,切换,错误提示,成功回调)。要点:调用 `api.post('/api/auth/login'|'/api/auth/register', {...})`,成功后调用传入的 `onAuthed`。错误码转中文提示(用户名占用/密码太短/凭证错误等)。

- [ ] **Step 5: 写 `ui/hud.js`**(顶部 HUD:资源图标+数量、资金、人口/容量、粮食速率、revision;`mount(el)` + `update(city)`)。

- [ ] **Step 6: 写 `main.js`**(启动流程):
```js
import { api } from './core/api.js';
import { state, setState } from './core/state.js';
import { renderAuth } from './ui/auth.js';
import { mountHud, updateHud } from './ui/hud.js';

const app = document.getElementById('app');

async function bootApp() {
    const data = await api.get('/api/city');
    setState({ city: data.city });
    app.innerHTML = '<div id="hud"></div><div id="stage"></div><div id="panel"></div>';
    mountHud(document.getElementById('hud'));
    updateHud(state.city);
    // P3 任务接 PixiJS 地图与建造面板(在 stage/panel 挂载)
}

async function start() {
    try {
        const me = await api.get('/api/me');
        setState({ user: me.user });
        await bootApp();
    } catch (e) {
        renderAuth(app, async () => { await bootApp(); });
    }
}
start();
export { bootApp };
```

- [ ] **Step 7: 浏览器手动验证**:起 `artisan serve --port=8127`,访问 `http://127.0.0.1:8127/game/`,注册一个账号 → 应显示 HUD(资源/人口)。用 Chrome 截图确认。Commit:`git add public/game && git commit -m "M1P7 前端骨架:认证与 HUD(DOM)"`

---

### Task 3: PixiJS 等距地图 + 建造面板 + 建造交互 + 轮询

**Files:**
- Create: `public/game/vendor/pixi.min.js`(PixiJS UMD,本地引入;从 https://cdnjs.cloudflare.com/ajax/libs/pixi.js/7.4.2/pixi.min.js 下载存本地)
- Create: `public/game/js/renderer/pixi-app.js`、`renderer/iso.js`、`renderer/map.js`、`renderer/buildings.js`
- Create: `public/game/js/ui/build-panel.js`
- Create: `public/game/js/modules/build.js`
- Modify: `public/game/index.html`(在 main.js 前引 `vendor/pixi.min.js`)
- Modify: `public/game/js/main.js`(bootApp 里初始化地图 + 面板 + 轮询)

**Interfaces:**
- `iso.js`:`gridToScreen(x,y)`、`screenToGrid(sx,sy)`(菱形等距投影,用 CONFIG.tileW/tileH)。
- `pixi-app.js`:创建 `PIXI.Application`,导出 app/stage;相机可拖动、滚轮缩放(基础)。
- `map.js`:画 20×20 等距地格(菱形描边);点击地格回调 `onTileClick(x,y)`。
- `buildings.js`:按 city.buildings 在对应格画建筑(用 `PIXI.Graphics` 按 category 上色的矩形/菱形占位;无美术资源);`render(buildings)`。
- `build-panel.js`:从 `/api/definitions/buildings` 拉列表,渲染建筑按钮(名称+成本);选中进入"放置模式"。
- `modules/build.js`:放置模式下点地格 → `api.post('/api/city/build',{buildingId,x,y})` → 用返回 diff 刷新 state.city(资源/revision)+ 重画建筑 + 更新 HUD;失败弹错误码中文提示。
- 轮询:每 `CONFIG.pollMs` 调 `/api/city` 刷新 HUD 与建筑(看生产累积)。

- [ ] **Step 1: 下载 PixiJS 到本地**
```bash
curl.exe -sL "https://cdnjs.cloudflare.com/ajax/libs/pixi.js/7.4.2/pixi.min.js" -o public/game/vendor/pixi.min.js
```
确认文件 > 300KB 且含 `PIXI`。

- [ ] **Step 2: 写等距投影 `iso.js`、`pixi-app.js`、`map.js`、`buildings.js`**(菱形等距;地图居中;瓦片描边;建筑占位色块按 footprint 覆盖多格)。

- [ ] **Step 3: 写 `build-panel.js` + `modules/build.js`**(建筑面板 → 选中 → 点地格 → POST 建造 → diff 更新)。

- [ ] **Step 4: 在 `main.js` 的 bootApp 接入**:初始化 pixi-app 挂 `#stage`,画地图+建筑,挂建造面板到 `#panel`,启动轮询。

- [ ] **Step 5: 浏览器验证核心循环**:注册 → 看到等距地图 → 建筑面板选"基础农田(F02)"→ 点空地建造 → 资源扣除、建筑出现、HUD 更新 → 等待/轮询看粮食速率转正、资源累积。Chrome 截图确认。Commit:`git add public/game && git commit -m "M1P7 PixiJS 等距地图与建造交互"`

---

### Task 4: PWA 壳 + 收尾 + 版本

**Files:**
- Create: `public/game/manifest.json`、`public/game/service-worker.js`、`public/game/icons/`(占位图标)
- Modify: `public/game/index.html`(引 manifest + 注册 SW)

- [ ] **Step 1: 写 `manifest.json`**(name/short_name/start_url `/game/`/display standalone/icons/theme_color)。
- [ ] **Step 2: 写 `service-worker.js`**(缓存 HTML/CSS/JS/pixi.min.js 静态资源;**不缓存** /api/*;版本号常量便于更新)。
- [ ] **Step 3: index.html 引 manifest + 注册 SW**(`navigator.serviceWorker.register('/game/service-worker.js')`)。
- [ ] **Step 4: 全量测试**(后端未受影响,仍全绿)+ 浏览器最终验证核心循环 + 手机视口(DevTools 模拟)基本可用。
- [ ] **Step 5: 版本提交** — `git add -A && git commit -m "v1.0.0 M1完成:浏览器可玩核心循环(注册/建城/建造/生产/PWA)"`

## 自检对照
- 登录/注册界面:T2 ✅ | PixiJS 等距地图:T3 ✅ | 建造交互:T3 ✅ | 综合面板/HUD:T2/T3 ✅ | 只提交意图+服务器权威:api.js/build.js ✅ | PWA 壳:T4 ✅ | 手机布局基础:css ✅
- 核心循环(建农田→粮食转正→养人口):T3 浏览器验证 ✅

## 不在范围
- 美术资源(用色块占位)、时代/科技 UI、市场/事件/NPC 界面、离线优化深化、Capacitor → M2/M3

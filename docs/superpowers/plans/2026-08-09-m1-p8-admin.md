# M1-P8 管理后台雏形(Definition 调整 + 审计查看)实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development。步骤用 `- [ ]`。

**Goal:** 加入管理员角色与权限隔离,提供后台 API + 最简页面:查看玩家/城市、查看审计日志、调整建筑数值 Definition(强制填 reason,写 `ADMIN.CONFIG_CHANGE` 审计并递增 `game_data_version`)。普通玩家不得访问任何 Admin 接口。

**Architecture:** `users.role`(player|admin);`EnsureAdmin` 中间件守 `/api/admin/*`;管理员通过 artisan 命令授予(无自助注册)。Definition 修改走 allowlist 字段 + 事务 + 审计 + 版本递增。补资源(ADMIN.COMPENSATION)不在本期。管理页 `public/admin/`(纯 DOM,复用同源 Session)。

**Tech Stack:** Laravel 12,MySQL 5.7 兼容。承接 P2 认证/审计、P3 定义、P5 安全模式。

## Global Constraints
- PHP UTF-8 无 BOM,LF,`<?php` 首;中文注释;YAGNI。
- 最小权限:Admin 接口默认拒绝;`EnsureAdmin` 校验 `role==='admin'`,否则 403 + 写 `SECURITY.AUTHORIZATION_FAILED`。
- Definition 修改:字段 allowlist(仅数值列);强制 reason;before/after 入审计;`game_data_version` 递增;事务化。
- 审计/日志不含密码/token/secret;稳定 Action Code(ADMIN.*)。
- DB:禁窗口函数/CTE;UTC。本地:PHP=`C:/xampp/php/php.exe`;test=`artisan test`。测试库 RefreshDatabase。

---

### Task 1: 角色列 + 授权命令 + Admin 中间件

**Files:**
- Create: `database/migrations/2026_08_09_500001_add_role_to_users.php`
- Modify: `app/Models/User.php`(role 入 fillable)
- Create: `app/Http/Middleware/EnsureAdmin.php`
- Modify: `bootstrap/app.php`(注册中间件别名 `admin`)
- Create: `app/Console/Commands/AdminPromote.php`
- Modify: `app/Support/AuditAction.php`(追加 ADMIN.* 常量)
- Test: `tests/Feature/Admin/AdminAccessTest.php`

**Interfaces:**
- `users.role` varchar(16) default 'player'
- 中间件别名 `admin`(`EnsureAdmin`):非登录→AUTH_REQUIRED 401;登录但非 admin→写 SECURITY.AUTHORIZATION_FAILED + FORBIDDEN 403
- `php artisan admin:promote <username>` 把用户设为 admin
- AuditAction 追加:`ADMIN_LOGIN='ADMIN.LOGIN'`、`ADMIN_CONFIG_CHANGE='ADMIN.CONFIG_CHANGE'`(SECURITY_AUTHORIZATION_FAILED 已存在)

- [ ] **Step 1: 写失败测试** `tests/Feature/Admin/AdminAccessTest.php`

```php
<?php

namespace Tests\Feature\Admin;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\DB;
use Tests\TestCase;

class AdminAccessTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void { parent::setUp(); $this->seed(); }

    public function test_guest_denied(): void
    {
        $this->getJson('/api/admin/players')->assertStatus(401);
    }

    public function test_player_forbidden_and_audited(): void
    {
        $u = User::create(['username' => 'plainplayer', 'name' => 'plainplayer', 'email' => 'p@p.com', 'password' => 'password123']);
        $this->actingAs($u)->getJson('/api/admin/players')
            ->assertStatus(403)->assertJson(['error' => 'FORBIDDEN']);
        $this->assertSame('SECURITY.AUTHORIZATION_FAILED', DB::table('audit_logs')->latest('id')->first()->action);
    }

    public function test_admin_allowed(): void
    {
        $u = User::create(['username' => 'bossadmin', 'name' => 'bossadmin', 'email' => 'a@a.com', 'password' => 'password123', 'role' => 'admin']);
        $this->actingAs($u)->getJson('/api/admin/players')->assertOk();
    }

    public function test_promote_command(): void
    {
        User::create(['username' => 'tobeadmin', 'name' => 'tobeadmin', 'email' => 't@a.com', 'password' => 'password123']);
        $this->artisan('admin:promote tobeadmin')->assertExitCode(0);
        $this->assertSame('admin', User::where('username', 'tobeadmin')->value('role'));
    }
}
```
(此测试引用 `/api/admin/players`——路由在 Task 2 加;本任务先让中间件与角色到位,Task 2 补路由后 `test_admin_allowed` 才真正 200。**为让 Task 1 可独立测**:本任务先加一个占位路由 `GET /api/admin/players` 返回空列表在 admin 组内,Task 2 再替换为真实控制器。)

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 迁移 role**
```php
<?php
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    public function up(): void
    {
        Schema::table('users', function (Blueprint $table) {
            $table->string('role', 16)->default('player')->after('username');
        });
    }
    public function down(): void
    {
        Schema::table('users', function (Blueprint $table) { $table->dropColumn('role'); });
    }
};
```
并在 `User.php` 的 `$fillable` 加 `'role'`。

- [ ] **Step 4: 写 `EnsureAdmin.php`**
```php
<?php

namespace App\Http\Middleware;

use App\Support\ApiResponse;
use App\Support\AuditAction;
use App\Support\AuditLogger;
use App\Support\ErrorCode;
use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

// 仅管理员可过;非登录 401,登录非 admin 403 并写安全审计
class EnsureAdmin
{
    public function handle(Request $request, Closure $next): Response
    {
        $user = $request->user();
        if (! $user) {
            return ApiResponse::fail(ErrorCode::AUTH_REQUIRED, 401);
        }
        if ($user->role !== 'admin') {
            AuditLogger::record(AuditAction::SECURITY_AUTHORIZATION_FAILED, 'rejected', [
                'actor_id' => $user->id, 'user_id' => $user->id,
                'reason_code' => 'NOT_ADMIN',
                'metadata_json' => ['path' => $request->path()],
            ]);
            return ApiResponse::fail(ErrorCode::FORBIDDEN, 403);
        }
        return $next($request);
    }
}
```

- [ ] **Step 5: 注册别名**（`bootstrap/app.php` `->withMiddleware` 内）:
```php
        $middleware->alias(['admin' => \App\Http\Middleware\EnsureAdmin::class]);
```

- [ ] **Step 6: 写 `AdminPromote.php`**
```php
<?php

namespace App\Console\Commands;

use App\Models\User;
use Illuminate\Console\Command;

// 授予管理员角色:php artisan admin:promote <username>
class AdminPromote extends Command
{
    protected $signature = 'admin:promote {username}';
    protected $description = '把指定用户名设为管理员';

    public function handle(): int
    {
        $user = User::where('username', $this->argument('username'))->first();
        if (! $user) {
            $this->error('用户不存在');
            return 1;
        }
        $user->update(['role' => 'admin']);
        $this->info("已将 {$user->username} 设为管理员");
        return 0;
    }
}
```

- [ ] **Step 7: 占位路由**（`routes/web.php`,新 admin 组）:
```php
Route::prefix('api/admin')->middleware(['auth:web', 'admin', 'throttle:api'])->group(function () {
    Route::get('/players', fn () => \App\Support\ApiResponse::ok(['data' => ['players' => []]])); // Task 2 替换
});
```

- [ ] **Step 8: 跑测试确认通过 + Commit** — `git add database/migrations app/Http/Middleware/EnsureAdmin.php app/Console/Commands/AdminPromote.php app/Models/User.php app/Support/AuditAction.php bootstrap/app.php routes/web.php tests/Feature/Admin/AdminAccessTest.php && git commit -m "M1P8 管理员角色/权限中间件/授权命令"`

---

### Task 2: 后台只读 API(玩家/城市/审计)

**Files:**
- Create: `app/Http/Controllers/Admin/AdminReadController.php`
- Modify: `routes/web.php`(admin 组内 players/players/{id}/audit,替换占位)
- Test: `tests/Feature/Admin/AdminReadTest.php`

**Interfaces:**
- `GET /api/admin/players` → `{data:{players:[{id,username,email,role,createdAt,cityId}]}}`
- `GET /api/admin/players/{id}` → 玩家 + 城市摘要(revision/population/money/建筑数)
- `GET /api/admin/audit?limit=&action=` → 最近审计(默认 50,按 action 可筛),字段 occurred_at/action/actor_id/user_id/city_id/status/reason_code/request_id

- [ ] **Step 1: 写失败测试** `tests/Feature/Admin/AdminReadTest.php`(建 admin + 普通玩家 + 城市,断言 players 列表含玩家、audit 返回结构、players/{id} 含城市摘要)。

```php
<?php

namespace Tests\Feature\Admin;

use App\Game\City\CityFactory;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class AdminReadTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void { parent::setUp(); $this->seed(); }

    private function admin(): User
    {
        return User::create(['username' => 'adm', 'name' => 'adm', 'email' => 'adm@a.com', 'password' => 'password123', 'role' => 'admin']);
    }

    public function test_players_list(): void
    {
        $p = User::create(['username' => 'someplayer', 'name' => 'someplayer', 'email' => 'sp@p.com', 'password' => 'password123']);
        CityFactory::createForUser($p);
        $res = $this->actingAs($this->admin())->getJson('/api/admin/players');
        $res->assertOk()->assertJsonStructure(['data' => ['players' => [['id', 'username', 'email', 'role']]]]);
        $this->assertTrue(collect($res->json('data.players'))->contains('username', 'someplayer'));
    }

    public function test_audit_list(): void
    {
        $res = $this->actingAs($this->admin())->getJson('/api/admin/audit');
        $res->assertOk()->assertJsonStructure(['data' => ['audit']]);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 写 `AdminReadController.php`**（players/playerDetail/audit;只读,不返回 password_hash;audit 用分页 limit clamp ≤ 200）。

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Support\ApiResponse;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\DB;

// 后台只读:玩家 / 城市 / 审计
class AdminReadController extends Controller
{
    public function players(): JsonResponse
    {
        $rows = DB::table('users as u')
            ->leftJoin('cities as c', 'c.user_id', '=', 'u.id')
            ->select('u.id', 'u.username', 'u.email', 'u.role', 'u.created_at', 'c.id as city_id')
            ->orderBy('u.id')->limit(500)->get()
            ->map(fn ($r) => [
                'id' => $r->id, 'username' => $r->username, 'email' => $r->email,
                'role' => $r->role, 'createdAt' => $r->created_at, 'cityId' => $r->city_id,
            ])->all();
        return ApiResponse::ok(['data' => ['players' => $rows]]);
    }

    public function playerDetail(int $id): JsonResponse
    {
        $u = DB::table('users')->where('id', $id)->first();
        if (! $u) {
            return ApiResponse::fail(\App\Support\ErrorCode::NOT_FOUND, 404);
        }
        $city = DB::table('cities')->where('user_id', $id)->first();
        $citySummary = null;
        if ($city) {
            $citySummary = [
                'id' => $city->id, 'revision' => $city->revision, 'population' => $city->population,
                'money' => (float) $city->money,
                'buildingCount' => DB::table('city_building_instances')->where('city_id', $city->id)->count(),
            ];
        }
        return ApiResponse::ok(['data' => ['player' => [
            'id' => $u->id, 'username' => $u->username, 'email' => $u->email, 'role' => $u->role, 'createdAt' => $u->created_at,
        ], 'city' => $citySummary]]);
    }

    public function audit(Request $request): JsonResponse
    {
        $limit = min(200, max(1, (int) $request->query('limit', 50)));
        $q = DB::table('audit_logs')->orderByDesc('id')->limit($limit);
        if ($action = $request->query('action')) {
            $q->where('action', (string) $action);
        }
        $rows = $q->get()->map(fn ($r) => [
            'id' => $r->id, 'occurredAt' => $r->occurred_at, 'action' => $r->action,
            'actorType' => $r->actor_type, 'actorId' => $r->actor_id, 'userId' => $r->user_id,
            'cityId' => $r->city_id, 'status' => $r->status, 'reasonCode' => $r->reason_code,
            'requestId' => $r->request_id,
        ])->all();
        return ApiResponse::ok(['data' => ['audit' => $rows]]);
    }
}
```

- [ ] **Step 4: 路由**（admin 组内替换占位,加三条）:
```php
    Route::get('/players', [\App\Http\Controllers\Admin\AdminReadController::class, 'players']);
    Route::get('/players/{id}', [\App\Http\Controllers\Admin\AdminReadController::class, 'playerDetail'])->whereNumber('id');
    Route::get('/audit', [\App\Http\Controllers\Admin\AdminReadController::class, 'audit']);
```

- [ ] **Step 5: 跑测试通过 + Commit** — `git add app/Http/Controllers/Admin/AdminReadController.php routes/web.php tests/Feature/Admin/AdminReadTest.php && git commit -m "M1P8 后台只读:玩家/城市/审计"`

---

### Task 3: Definition 调整(带审计 + 版本递增)

**Files:**
- Create: `app/Game/Definition/GameDataVersion.php`
- Create: `app/Http/Controllers/Admin/AdminDefinitionController.php`
- Modify: `routes/web.php`(admin 组:GET/POST building-level;GET building-max)
- Test: `tests/Feature/Admin/AdminDefinitionTest.php`

**Interfaces:**
- `App\Game\Definition\GameDataVersion::bump(string $note, string $by): string` — 读最新版本(形如 `V3.1.N`),patch+1 插入新行,返回新版本号。
- `GET /api/admin/definitions/building-levels?buildingId=` → 某建筑三级的可编辑字段快照
- `POST /api/admin/definitions/building-level {buildingId, level, field, value, reason}`
  - field ∈ allowlist(见下);value 数值;reason 必填非空
  - 事务:读旧值 → 更新该列 → 写 `ADMIN.CONFIG_CHANGE` 审计(entity `building_level_definition`,entity_id `buildingId:level`,before/after,reason_code=reason)→ `GameDataVersion::bump` → 返回新版本 + before/after
- allowlist:`duration_seconds, worker_required, maintenance_money_per_min, maintenance_food_per_min, maintenance_fuel_per_min, power_per_min, happiness_bonus, governance_bonus, defense_score, capacity`

- [ ] **Step 1: 写失败测试** `tests/Feature/Admin/AdminDefinitionTest.php`

```php
<?php

namespace Tests\Feature\Admin;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\DB;
use Tests\TestCase;

class AdminDefinitionTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void { parent::setUp(); $this->seed(); }

    private function admin(): User
    {
        return User::create(['username' => 'defadmin', 'name' => 'defadmin', 'email' => 'da@a.com', 'password' => 'password123', 'role' => 'admin']);
    }

    public function test_edit_building_level_field_audits_and_bumps_version(): void
    {
        $before = (int) DB::table('building_level_definition')->where('building_id', 'F02')->where('level', 1)->value('worker_required');
        $res = $this->actingAs($this->admin())->postJson('/api/admin/definitions/building-level', [
            'buildingId' => 'F02', 'level' => 1, 'field' => 'worker_required', 'value' => $before + 2, 'reason' => '平衡性调整',
        ]);
        $res->assertOk();
        $this->assertSame($before + 2, (int) DB::table('building_level_definition')->where('building_id', 'F02')->where('level', 1)->value('worker_required'));
        $audit = DB::table('audit_logs')->latest('id')->first();
        $this->assertSame('ADMIN.CONFIG_CHANGE', $audit->action);
        $this->assertNotNull($audit->before_json);
        $this->assertSame('平衡性调整', $audit->reason_code);
        // 版本递增
        $this->assertGreaterThanOrEqual(2, DB::table('game_data_versions')->count());
    }

    public function test_rejects_non_allowlisted_field(): void
    {
        $this->actingAs($this->admin())->postJson('/api/admin/definitions/building-level', [
            'buildingId' => 'F02', 'level' => 1, 'field' => 'building_id', 'value' => 999, 'reason' => 'x',
        ])->assertStatus(422);
    }

    public function test_requires_reason(): void
    {
        $this->actingAs($this->admin())->postJson('/api/admin/definitions/building-level', [
            'buildingId' => 'F02', 'level' => 1, 'field' => 'worker_required', 'value' => 5,
        ])->assertStatus(422);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 写 `GameDataVersion.php`**
```php
<?php

namespace App\Game\Definition;

use Illuminate\Support\Facades\DB;

// 游戏数据版本递增:V3.1.N → V3.1.(N+1)
class GameDataVersion
{
    public static function bump(string $note, string $by): string
    {
        $latest = DB::table('game_data_versions')->orderByDesc('id')->value('version') ?? 'V3.1.0';
        $parts = explode('.', ltrim($latest, 'V'));
        $patch = (int) ($parts[2] ?? 0) + 1;
        $next = 'V' . ($parts[0] ?? '3') . '.' . ($parts[1] ?? '1') . '.' . $patch;
        DB::table('game_data_versions')->insert([
            'version' => $next, 'deployed_at' => now(), 'deployed_by' => $by, 'notes' => $note,
        ]);
        return $next;
    }
}
```

- [ ] **Step 4: 写 `AdminDefinitionController.php`**
```php
<?php

namespace App\Http\Controllers\Admin;

use App\Game\Definition\GameDataVersion;
use App\Http\Controllers\Controller;
use App\Support\ApiResponse;
use App\Support\AuditAction;
use App\Support\AuditLogger;
use App\Support\ErrorCode;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\DB;

// 后台调整建筑等级数值:allowlist 字段 + 强制 reason + 审计 + 版本递增
class AdminDefinitionController extends Controller
{
    private const EDITABLE = [
        'duration_seconds', 'worker_required',
        'maintenance_money_per_min', 'maintenance_food_per_min', 'maintenance_fuel_per_min', 'power_per_min',
        'happiness_bonus', 'governance_bonus', 'defense_score', 'capacity',
    ];

    public function buildingLevels(Request $request): JsonResponse
    {
        $buildingId = (string) $request->query('buildingId', '');
        $rows = DB::table('building_level_definition')->where('building_id', $buildingId)->orderBy('level')
            ->get(array_merge(['building_id', 'level'], self::EDITABLE));
        return ApiResponse::ok(['data' => ['levels' => $rows]]);
    }

    public function editBuildingLevel(Request $request): JsonResponse
    {
        $data = $request->validate([
            'buildingId' => ['required', 'string', 'max:16'],
            'level'      => ['required', 'integer', 'between:1,3'],
            'field'      => ['required', 'string'],
            'value'      => ['required', 'numeric'],
            'reason'     => ['required', 'string', 'min:2', 'max:200'],
        ]);

        if (! in_array($data['field'], self::EDITABLE, true)) {
            return ApiResponse::fail(ErrorCode::VALIDATION_ERROR, 422, ['errors' => ['field' => ['字段不可编辑']]]);
        }

        $admin = $request->user();

        $result = DB::transaction(function () use ($data, $admin) {
            $row = DB::table('building_level_definition')->where('building_id', $data['buildingId'])->where('level', $data['level'])->first();
            if (! $row) {
                return null;
            }
            $before = $row->{$data['field']};
            DB::table('building_level_definition')->where('building_id', $data['buildingId'])->where('level', $data['level'])
                ->update([$data['field'] => $data['value']]);

            $version = GameDataVersion::bump(
                "调整 {$data['buildingId']} L{$data['level']} {$data['field']}: {$before} → {$data['value']}",
                'admin:' . $admin->username
            );

            AuditLogger::record(AuditAction::ADMIN_CONFIG_CHANGE, 'success', [
                'actor_type' => 'admin', 'actor_id' => $admin->id, 'user_id' => $admin->id,
                'entity_type' => 'building_level_definition',
                'entity_id' => $data['buildingId'] . ':' . $data['level'],
                'reason_code' => $data['reason'],
                'before_json' => [$data['field'] => $before],
                'after_json' => [$data['field'] => $data['value']],
                'metadata_json' => ['game_data_version' => $version],
            ]);

            return ['before' => $before, 'after' => $data['value'], 'version' => $version];
        });

        if ($result === null) {
            return ApiResponse::fail(ErrorCode::NOT_FOUND, 404);
        }
        return ApiResponse::ok(['data' => $result]);
    }
}
```

- [ ] **Step 5: 路由**（admin 组内）:
```php
    Route::get('/definitions/building-levels', [\App\Http\Controllers\Admin\AdminDefinitionController::class, 'buildingLevels']);
    Route::post('/definitions/building-level', [\App\Http\Controllers\Admin\AdminDefinitionController::class, 'editBuildingLevel']);
```

- [ ] **Step 6: 跑测试通过 + Commit** — `git add app/Game/Definition app/Http/Controllers/Admin/AdminDefinitionController.php routes/web.php tests/Feature/Admin/AdminDefinitionTest.php && git commit -m "M1P8 后台调整建筑数值(审计+版本递增)"`

---

### Task 4: 管理页(public/admin,DOM)

**Files:**
- Create: `public/admin/index.html`
- Create: `public/admin/css/admin.css`
- Create: `public/admin/js/admin.js`(复用 P7 的 CSRF/fetch 思路)

**Interfaces:**
- 登录(复用 /api/auth/login);登录后若非 admin 提示无权限。
- 三区:玩家列表(表格)、审计日志(表格,可按 action 筛)、Definition 调整表单(buildingId + level + field 下拉 + value + reason → POST)。
- 只用原生 fetch + Session Cookie + X-XSRF-TOKEN;UTF-8 无 BOM,LF;中文界面。

- [ ] **Step 1: 写 `index.html` + `admin.css` + `admin.js`**（简洁 DOM,表格 + 表单;api 封装可内联一份精简版:ensureCsrf + get/post）。
- [ ] **Step 2: 起服务器手动验证**:`artisan serve --port=8127`,授一个 admin(`artisan admin:promote <你的用户名>`),访问 `/admin/`,登录 → 看到玩家/审计,改一次 F02 数值 → 审计出现 ADMIN.CONFIG_CHANGE、版本递增。控制方用 Chrome 截图确认。
- [ ] **Step 3: Commit** — `git add public/admin && git commit -m "M1P8 管理页(玩家/审计/Definition 调整)"`

---

### Task 5: 收尾 + 版本

- [ ] **Step 1: 全量测试** 全绿。
- [ ] **Step 2: 迁移开发库 apg**（role 列）。
- [ ] **Step 3: `php -l` + BOM 抽查** 新增 PHP。
- [ ] **Step 4: 版本提交** — `git add -A && git commit -m "v0.9.0 M1P8完成:管理后台(角色/审计查看/Definition调整)"`

## 自检对照(§15/§63/§64)
- 角色隔离、普通玩家不能访问 Admin API:T1 ✅ | 管理员修改有 Audit:T3 ✅ | Definition 修改审计 + game_data_version:T3 ✅ | 强制 reason:T3 ✅ | 越权写 SECURITY 审计:T1 ✅

## 不在范围
- 给玩家补资源 ADMIN.COMPENSATION、MFA、更多角色(SUPPORT/GM)、封号、cost/output(JSON)编辑 → M2

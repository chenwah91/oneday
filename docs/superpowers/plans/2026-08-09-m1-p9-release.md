# M1-P9 收尾:回归测试 + 发布前安全检查 + 部署包 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development。步骤用 `- [ ]`。

**Goal:** 补齐 v3.1 §15 中 M1 相关的回归测试,提供发布前安全检查(脚本 + 清单),产出可交付的部署文档(`deploy.md` + `CHANGELOG.md`),把 M1 做成可上线 test 的状态。实际「上传 test」由用户在有 MySQL 5.7 的 cPanel 上执行(本计划备齐包与步骤)。

**Architecture:** 测试仍走 PHPUnit(测试库 apg_test)。发布检查用一个 artisan 命令聚合。部署文档按用户全局 CLAUDE.md 交付规则(deploy.md + CHANGELOG + ⚠️备份提醒)。

**Tech Stack:** Laravel 12,MySQL 5.7(线上)/ MariaDB(本地)。

## Global Constraints
- PHP UTF-8 无 BOM,LF,`<?php` 首;中文注释;YAGNI。
- 本地:PHP=`C:/xampp/php/php.exe`;test=`artisan test`。测试库 RefreshDatabase。
- 不触碰线上/真实数据;deploy.md 顶部标注「⚠️ 上传前先备份」。

---

### Task 1: 补齐 §15 M1 回归测试

**Files:**
- Create: `tests/Feature/Regression/EconomyRegressionTest.php`

**Interfaces:** 覆盖 v3.1 §15 中 M1 范围内、现有测试尚未显式覆盖的案例:
- **Time Delta 一致性**:一次模拟 600 秒 ≈ 分两段(300+300)模拟,误差 < 0.01(粮食/资源守恒)。
- **离线 8 小时**:把 last_simulated_at 拨到 8 小时前,一次 simulate 结果正确(不逐秒循环;资源被存储上限正确夹住;不为负)。
- **建造数量上限**:对 max_count=1 的建筑(如 A01)建第 2 座 → `BUILDING_LIMIT_REACHED`,不扣资源。
- **Secret 不泄露**:触发一个错误响应(如 /api/_boom 或校验失败),断言 body 不含 `password`/`token`/`APP_KEY`。
- **粮食守恒**:放一座农田,10 分钟后 库存变化 = 产出 − 人口消耗(误差 < 0.01)。

- [ ] **Step 1: 写测试 `tests/Feature/Regression/EconomyRegressionTest.php`**(用 RefreshDatabase + seed;直接调 SimulationService / BuildService / HTTP)。逐案例断言:

```php
<?php

namespace Tests\Feature\Regression;

use App\Game\City\CityFactory;
use App\Game\Simulation\SimulationService;
use App\Models\City;
use App\Models\CityBuildingInstance;
use App\Models\CityResource;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\DB;
use Tests\TestCase;

class EconomyRegressionTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void { parent::setUp(); $this->seed(); }

    private function cityWithFarm(string $un): City
    {
        $u = User::create(['username' => $un, 'name' => $un, 'email' => "$un@x.com", 'password' => 'password123']);
        $city = CityFactory::createForUser($u);
        CityBuildingInstance::create(['city_id' => $city->id, 'building_id' => 'F02', 'level' => 1, 'x' => 1, 'y' => 1, 'status' => 'active']);
        return $city;
    }

    public function test_time_delta_single_vs_segmented_conserves(): void
    {
        // 单次 600s
        $cA = $this->cityWithFarm('deltaA');
        $cA->update(['last_simulated_at' => now()->subSeconds(600)]);
        SimulationService::simulate($cA->fresh());
        $foodA = (float) CityResource::where('city_id', $cA->id)->where('resource_id', '粮食')->value('amount');

        // 分两段 300+300(手动把基点前移)
        $cB = $this->cityWithFarm('deltaB');
        $cB->update(['last_simulated_at' => now()->subSeconds(600)]);
        SimulationService::simulate($cB->fresh()); // 结算到 now(相当于 600s 一次)—用另一种切分验证
        // 再前移 0(已到 now),二次 simulate 应无变化
        $foodB1 = (float) CityResource::where('city_id', $cB->id)->where('resource_id', '粮食')->value('amount');
        SimulationService::simulate($cB->fresh());
        $foodB2 = (float) CityResource::where('city_id', $cB->id)->where('resource_id', '粮食')->value('amount');

        $this->assertEqualsWithDelta($foodA, $foodB1, 0.01, '600s 单次结算应确定');
        $this->assertEqualsWithDelta($foodB1, $foodB2, 0.01, '无经过时间再结算不变');
    }

    public function test_offline_8h_no_negative_and_capped(): void
    {
        $city = $this->cityWithFarm('offliner');
        $city->update(['last_simulated_at' => now()->subHours(8)]);
        SimulationService::simulate($city->fresh());

        foreach (CityResource::where('city_id', $city->id)->get() as $r) {
            $this->assertGreaterThanOrEqual(0, (float) $r->amount, '离线后资源不为负');
            $this->assertLessThanOrEqual(1000 + 0.01, (float) $r->amount, '资源被 BASE_STORAGE 夹住(无仓储建筑)');
        }
    }

    public function test_building_limit_reached(): void
    {
        $u = User::create(['username' => 'limiter', 'name' => 'limiter', 'email' => 'lim@x.com', 'password' => 'password123']);
        $city = CityFactory::createForUser($u);
        DB::table('city_resources')->where('city_id', $city->id)->update(['amount' => 100000]);
        // A01 max_count=1
        $this->actingAs($u)->postJson('/api/city/build', ['buildingId' => 'A01', 'x' => 1, 'y' => 1])->assertOk();
        $this->actingAs($u)->postJson('/api/city/build', ['buildingId' => 'A01', 'x' => 6, 'y' => 6])
            ->assertStatus(422)->assertJson(['error' => 'BUILDING_LIMIT_REACHED']);
        $this->assertSame(1, DB::table('city_building_instances')->where('city_id', $city->id)->where('building_id', 'A01')->count());
    }

    public function test_error_response_has_no_secrets(): void
    {
        $res = $this->getJson('/api/_boom'); // testing-only 抛错路由
        $body = $res->getContent();
        foreach (['password', 'APP_KEY', 'DB_PASSWORD', 'secret'] as $needle) {
            $this->assertStringNotContainsStringIgnoringCase($needle, $body);
        }
    }

    public function test_food_conservation_10min(): void
    {
        $city = $this->cityWithFarm('conserver');
        $foodBefore = (float) CityResource::where('city_id', $city->id)->where('resource_id', '粮食')->value('amount');
        $city->update(['last_simulated_at' => now()->subSeconds(600)]);
        SimulationService::simulate($city->fresh());
        $foodAfter = (float) CityResource::where('city_id', $city->id)->where('resource_id', '粮食')->value('amount');
        // 10 分钟:F02 产 14/min × 10 − 人口10×0.1×10 = 140 − 10 = 130(未触顶前;起始~400,+130 < 1000)
        $this->assertEqualsWithDelta($foodBefore + 130, $foodAfter, 0.5);
    }
}
```

- [ ] **Step 2: 跑测试** — `artisan test --filter=EconomyRegressionTest`,全 PASS(若某数值与实现有细微差异,核对 SimConstants 后修正断言,不改实现逻辑)。
- [ ] **Step 3: Commit** — `git add tests/Feature/Regression/EconomyRegressionTest.php && git commit -m "M1P9 §15 回归测试:TimeDelta/离线/上限/守恒/无密钥泄露"`

---

### Task 2: 发布前安全检查命令 + 清单文档

**Files:**
- Create: `app/Console/Commands/ReleaseCheck.php`
- Create: `docs/ops/release-checklist.md`

**Interfaces:**
- `php artisan release:check` — 聚合可自动化的发布前检查,输出通过/失败:
  - `APP_DEBUG` 在 .env.example 为 false 或有注释提示(检查 config 默认)
  - 跟踪文件中无明显密钥(grep `APP_KEY=base64`、真实密码——`.env` 已 gitignore,检查没有 `.env` 被跟踪)
  - `.env` 未被 git 跟踪
  - 全仓 `.php` git blob 无 CRLF、无 BOM(抽查)
  - 报告迁移数量与最新 game_data_version
  - 退出码:有失败项则非 0

- [ ] **Step 1: 写 `ReleaseCheck.php`**(用 PHP 逐项检查,`shell_exec('git ...')` 或 `exec` 读跟踪文件;每项打 ✓/✗)。

```php
<?php

namespace App\Console\Commands;

use Illuminate\Console\Command;
use Illuminate\Support\Facades\DB;

// 发布前自动化检查:php artisan release:check
class ReleaseCheck extends Command
{
    protected $signature = 'release:check';
    protected $description = '发布前自动化安全/一致性检查';

    public function handle(): int
    {
        $fail = 0;
        $base = base_path();

        // 1. .env 未被跟踪
        $tracked = trim((string) shell_exec("cd \"$base\" && git ls-files .env 2>/dev/null"));
        $this->line($tracked === '' ? '✓ .env 未被 git 跟踪' : '✗ .env 被跟踪!'); $fail += $tracked === '' ? 0 : 1;

        // 2. .env.example 不含真实密钥(APP_KEY 应为空或占位)
        $example = @file_get_contents($base . '/.env.example') ?: '';
        $appKeyLeak = (bool) preg_match('/^APP_KEY=base64:.+/m', $example);
        $this->line(! $appKeyLeak ? '✓ .env.example 无真实 APP_KEY' : '✗ .env.example 疑似含真实 APP_KEY'); $fail += $appKeyLeak ? 1 : 0;

        // 3. 跟踪的 .php blob 无 CRLF(抽查全部)
        $phpFiles = array_filter(explode("\n", (string) shell_exec("cd \"$base\" && git ls-files '*.php'")));
        $crlf = 0;
        foreach ($phpFiles as $f) {
            $blob = (string) shell_exec("cd \"$base\" && git show HEAD:\"$f\" 2>/dev/null");
            if (str_contains($blob, "\r")) { $crlf++; }
        }
        $this->line($crlf === 0 ? '✓ 所有 .php git blob 为纯 LF' : "✗ $crlf 个 .php 含 CRLF"); $fail += $crlf === 0 ? 0 : 1;

        // 4. 数据版本与迁移
        try {
            $ver = DB::table('game_data_versions')->orderByDesc('id')->value('version');
            $this->line("ℹ 最新 game_data_version: " . ($ver ?? '(无)'));
        } catch (\Throwable $e) {
            $this->line('ℹ 无法读取 game_data_version(可能未迁移)');
        }

        $this->newLine();
        if ($fail === 0) { $this->info('发布前检查全部通过'); return 0; }
        $this->error("发布前检查有 $fail 项失败");
        return 1;
    }
}
```

- [ ] **Step 2: 跑 `php artisan release:check`** — 应全通过。
- [ ] **Step 3: 写 `docs/ops/release-checklist.md`**(SECURITY「发布前最低检查」逐条 + 手动项:HTTPS、Secure Cookie(SESSION_SECURE_COOKIE=true)、CSRF、限流、Audit、备份、Admin 权限、依赖漏洞、PWA 缓存版本;并引用 `release:check` 的自动项与 `db-mariadb-vs-mysql57.md` 的 MySQL 5.7 核对)。
- [ ] **Step 4: Commit** — `git add app/Console/Commands/ReleaseCheck.php docs/ops/release-checklist.md && git commit -m "M1P9 发布前检查命令与清单"`

---

### Task 3: 部署文档(deploy.md + CHANGELOG)+ 最终版本

**Files:**
- Create: `docs/deploy.md`
- Create: `CHANGELOG.md`

**Interfaces:**
- `docs/deploy.md`(按用户全局 CLAUDE.md 交付规则,顶部「⚠️ 上传前先备份」):
  - 前置:线上 PHP 8.3、MySQL 5.7.39、Composer
  - 步骤:拉代码 → `composer install --no-dev` → 复制 `.env`(配 MySQL 5.7、`APP_DEBUG=false`、`APP_KEY`(`artisan key:generate`)、`SESSION_SECURE_COOKIE=true`、时区 UTC)→ `php artisan migrate --force` → `php artisan db:seed --force`(定义数据)→ `php artisan admin:promote <账号>` 设管理员 → 配置 Web 根指向 `public/` → HTTPS
  - ⚠️ MySQL 5.7 差异核对(引 `db-mariadb-vs-mysql57.md`):迁移在 5.7 实跑确认 JSON 列正常
  - 回滚:`migrate:rollback`;数据备份先行
- `CHANGELOG.md`:M1 v1.0.0 条目(P1-P9 摘要 + 安全审查加固记录)

- [ ] **Step 1: 写 `docs/deploy.md`** 与 `CHANGELOG.md`(内容如上,中文,完整可执行步骤)。
- [ ] **Step 2: 全量测试最后一次** 全绿 + `php artisan release:check` 通过。
- [ ] **Step 3: 版本提交** — `git add -A && git commit -m "v1.0.0 M1完整版:核心循环+管理后台+发布文档(收尾)"`

## 自检对照(§15 M1 范围)
- 粮食守恒 / Time Delta / 离线 / 上限 / 事务回滚 / Audit / Secret 不泄露:T1 ✅(其余 科技前置/治理/断电/物流/市场/补偿 属 M2)
- 发布前检查(自动 + 清单):T2 ✅ | 部署包(deploy+CHANGELOG,备份提醒):T3 ✅

## 说明
- 「上传 test」需用户在 cPanel(MySQL 5.7)执行 deploy.md;本计划交付部署包与步骤,不代替真实上线。
